'''
As listas, assim como as variáveis, devem seguir as regras e a padronização em relação aos seus
identificadores (nomes), ou seja, nada de usar os seguintes nomes para as listas: while, for, n@me, 1berto, entre
outros identificadores inválidos.
'''

#Todas as listas podem ser inicializadas vazias ou com dados.

#Lista preenchida estaticamente:

lista_estatica = ["xpto", True]

#Lista preenchida dinamicamente:

lista_dinamica = [input("Digite o usuário: "), bool(int(input("Está logado? ")))]

#Lista vazia:

lista_vazia = []


