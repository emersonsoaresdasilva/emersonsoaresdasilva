//Declarando e inicializando a variável de controle.
	int main(void)
{
	int contador = 1;
	
	//Testando a condição.
	while(contador <=100){
	    
	//Executando um comando dentro do laço.
	printf("\t%d", contador);
	
	//Atualizando a variável de controle.
	contador++;
}
    //Retorna a tecla digitada.
    getch();
}